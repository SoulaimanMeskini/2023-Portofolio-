import React from 'react';
import styled from 'styled-components';

export const Lines = () => {
  
  


  return (
    <LineContainer>
    </LineContainer>
  );
};

const LineContainer = styled.div`
`;
